import 'package:flutter/material.dart';

class {{pascalCase name}}View extends StatelessWidget {
  const {{pascalCase name}}View({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}