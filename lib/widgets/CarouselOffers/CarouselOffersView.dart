import 'package:flutter/material.dart';

class CarouselOffersView extends StatelessWidget {
  const CarouselOffersView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}