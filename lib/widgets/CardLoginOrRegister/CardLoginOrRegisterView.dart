import 'package:flutter/material.dart';

class CardLoginOrRegisterView extends StatelessWidget {
  const CardLoginOrRegisterView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}